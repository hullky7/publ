0. Framework allows dimensions from 3 x 3 to 9 x 9 (defined by the constants DIM_MIN and DIM_MAX).
1. The board is represented by a 2 dimensional array.
2. Function "usleep" is called to greet the player at game’s start.
3. It's needed to implement init(), draw(), move(), won() functions.